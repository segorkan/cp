#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	int n;
	cin >> n;
	vector <int> myarr;
	for (int i = 0; i < n; i++) {
		int h;
		cin >> h;
		myarr.push_back(h);
	}
	vector<int> v(10, 0);
	for (auto el : myarr)
		++v[el];
	myarr.resize(0);
	for (int d = 0; d < v.size(); ++d)
		myarr.insert(myarr.end(), v[d], d);
	for (int i = 0; i < n; i++) cout << myarr[i] << ' ';
}
